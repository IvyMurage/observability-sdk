apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-gateway
  namespace: observability
  labels:
    release: prometheus
    app: api-gateway
spec:
  namespaceSelector:
    matchNames:
      - tugane-sit
  selector:
    matchLabels:
      app: api-gateway
  endpoints:
    - port: http
      path: /api/metrics
      interval: 15s
      scrapeTimeout: 10s
